# Runbook profile: classy (verify-only prereqs; classifier API + macOS UI; depends on teller).

# --- 01_install_prerequisites (verify-only, no Homebrew installs) ---
PREREQ_MODE="verify"
PREREQ_BANNER="Checking classy prerequisites"
PREREQ_VERIFY_COMMANDS="python3.12|python3.12 (or python3)
swift|swift (Xcode command line tools)
mkcert|mkcert (brew install mkcert) for local classifier API TLS
kcov|kcov (brew install kcov) for shell coverage mode
1psa|1psa (1Password CLI shim) for CLASSY_WRITE_TOKEN + DB password
sqlcipher|sqlcipher (brew install sqlcipher) for pysqlcipher3"
PREREQ_VERIFY_SQLCIPHER_HEADERS="true"
PREREQ_SUCCESS_GUIDANCE="Next: ./02_create_venv.sh && source classy-venv/bin/activate && ./04_load_requirements.sh"
PREREQ_FINAL_GUIDANCE="Next: ./02_create_venv.sh && source classy-venv/bin/activate && ./04_load_requirements.sh
  ./04_install_classifier_api_tls.sh"

# --- 02_create_venv ---
VENV_EXISTS_POLICY="continue"
INSTALL_VENV_TEST_CACHE="true"
ACTIVATION_HINT="activate"
REQUIRE_PREREQ_SCRIPT="true"

# --- 04_load_requirements (classy wrapper: SQLCipher build + editable teller, requirements.in fallback) ---
LOAD_REQUIREMENTS_FILES="requirements.txt requirements.in"
LOAD_REQUIREMENTS_BOOTSTRAP_PIP="false"
LOAD_REQUIREMENTS_SQLCIPHER_BUILD="true"
LOAD_REQUIREMENTS_EDITABLE_SIBLINGS="${CLASSY_TELLER_DIR:-../teller}"

# --- orchestrator 08_run_all_tests_parallel (parallel autodiscovery of tests/t*.sh pointers) ---
# Lanes are discovered from tests/ (no hardcoded list), so renames/renumbers just work.
# classy's tests/tNN_ files are themselves the pointers (shared lanes) or self-contained lanes (owned).
ORCHESTRATOR_MODE="parallel-discover"
# DAST (t10): classy's classifier API entrypoint + skip teller-DB seeding/integrity sub-steps.
DAST_APP_SCRIPT="06_run_classification_api.py"
DAST_DB_INTEGRATION="false"
WRITE_TOKEN_PSA_ITEM="CLASSY_WRITE_TOKEN"
WRITE_TOKEN_HEADER_NAME="X-Classy-Write-Token"
# 04_install_classifier_api_tls: knobs for the generic install_api_tls golden (legacy CLASSY_TLS_* honored).
API_TLS_LABEL="classifier API"
API_TLS_DIR="${CLASSY_TLS_DIR:-$HOME/.classy}"
API_TLS_CERT_FILE="${CLASSY_TLS_CERT_FILE:-${API_TLS_DIR}/classifier-localhost-cert.pem}"
API_TLS_KEY_FILE="${CLASSY_TLS_KEY_FILE:-${API_TLS_DIR}/classifier-localhost-key.pem}"
API_TLS_FORCE_REGENERATE="${CLASSY_TLS_FORCE_REGENERATE:-false}"
# Keep legacy TLS env names aligned so shared DAST helpers resolve the same cert/key.
CLASSY_TLS_CERT_FILE="${CLASSY_TLS_CERT_FILE:-${API_TLS_CERT_FILE}}"
CLASSY_TLS_KEY_FILE="${CLASSY_TLS_KEY_FILE:-${API_TLS_KEY_FILE}}"
TELLER_CLASSIFIER_TLS_CERT_FILE="${TELLER_CLASSIFIER_TLS_CERT_FILE:-${API_TLS_CERT_FILE}}"
TELLER_CLASSIFIER_TLS_KEY_FILE="${TELLER_CLASSIFIER_TLS_KEY_FILE:-${API_TLS_KEY_FILE}}"
# t00: ignore pydantic validator receiver `cls` (vulture false-positive on @field_validator classmethods).
VULTURE_EXTRA_ARGS="--ignore-names cls"
# t11 fuzz: classy property tests use smaller per-test example counts than teller's defaults.
FUZZ_MIN_PER_TEST_RATIO_PERCENT="10"
FUZZ_MIN_TOTAL_EXAMPLES="1500"
# t02: classy pins direct deps in requirements.in (no generated requirements.txt).
DEPENDENCY_REQUIREMENTS_FILE="requirements.in"
# t02: teller is installed editable (sibling dependency), intentional non-cruft.
DEPENDENCY_VENV_CRUFT_ALLOW="teller"
# t06: Python line coverage policy for classy unit-test lane.
RUN_PYTEST_COVERAGE="true"
COVERAGE_REPORT_DIR="./artifacts/coverage"
COVERAGE_SOURCE_DIRS="src/classy"
# t09: mutation lane needs a larger wall-clock budget in classy's module set.
MUTATION_TIMEOUT_SECONDS="5400"
# t09: ratcheted threshold after local improvements (70.32% latest); continue to 75/80 via CI-only mutation runs.
MUTATION_SCORE_THRESHOLD="65"
# t09: execute mutants concurrently per source file to keep runtime bounded.
MUTATION_WORKERS="8"
# t09: keep runtime bounded by relying on expanded mutmut tests_dir instead of full-suite survivor reruns.
MUTATION_FULL_SUITE_ESCALATION="false"
# t09: curated proven-equivalent survivors excluded from score denominator.
MUTATION_EQUIVALENTS_FILE="tests/mutation_equivalents.txt"
# shared pointer-contract requirements + bats are centralized in runner.
TRACEABILITY_REQUIREMENTS_ROOTS="${RUNBOOK_REPO_ROOT}/requirements:${RUNNER_HOME}/shared/requirements/pointers/classy"
TRACEABILITY_TEST_ROOTS="${RUNBOOK_REPO_ROOT}/tests/sh:${RUNNER_HOME}/shared/tests/sh/pointers/classy"
SHELL_BATS_ROOTS="${TRACEABILITY_TEST_ROOTS}"

export PREREQ_MODE PREREQ_BANNER PREREQ_VERIFY_COMMANDS PREREQ_VERIFY_SQLCIPHER_HEADERS \
  PREREQ_SUCCESS_GUIDANCE PREREQ_FINAL_GUIDANCE VENV_EXISTS_POLICY INSTALL_VENV_TEST_CACHE ACTIVATION_HINT \
  REQUIRE_PREREQ_SCRIPT LOAD_REQUIREMENTS_FILES LOAD_REQUIREMENTS_BOOTSTRAP_PIP \
  LOAD_REQUIREMENTS_SQLCIPHER_BUILD LOAD_REQUIREMENTS_EDITABLE_SIBLINGS \
  ORCHESTRATOR_MODE DAST_APP_SCRIPT DAST_DB_INTEGRATION \
  WRITE_TOKEN_PSA_ITEM WRITE_TOKEN_HEADER_NAME \
  API_TLS_LABEL API_TLS_DIR API_TLS_CERT_FILE API_TLS_KEY_FILE API_TLS_FORCE_REGENERATE \
  CLASSY_TLS_CERT_FILE CLASSY_TLS_KEY_FILE TELLER_CLASSIFIER_TLS_CERT_FILE TELLER_CLASSIFIER_TLS_KEY_FILE \
  VULTURE_EXTRA_ARGS FUZZ_MIN_PER_TEST_RATIO_PERCENT FUZZ_MIN_TOTAL_EXAMPLES \
  DEPENDENCY_REQUIREMENTS_FILE DEPENDENCY_VENV_CRUFT_ALLOW RUN_PYTEST_COVERAGE COVERAGE_REPORT_DIR COVERAGE_SOURCE_DIRS \
  MUTATION_TIMEOUT_SECONDS MUTATION_SCORE_THRESHOLD MUTATION_WORKERS \
  MUTATION_FULL_SUITE_ESCALATION MUTATION_EQUIVALENTS_FILE \
  TRACEABILITY_REQUIREMENTS_ROOTS TRACEABILITY_TEST_ROOTS SHELL_BATS_ROOTS
