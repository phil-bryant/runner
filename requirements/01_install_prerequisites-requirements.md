# 01 install prerequisites Requirements

## Scope

Requirements-only mode: true

Applies to `01_install_prerequisites.sh`. This is a shared golden setup entrypoint owned by the runner engine and exercised end-to-end by the consuming repos (teller/classy/matchy/mailcart) where its behavioral source/test traceability is enforced. Runner records it here as a self-run inventory entry so the numbered-script coverage and scope-alignment checks remain satisfied without duplicating downstream enforcement.
