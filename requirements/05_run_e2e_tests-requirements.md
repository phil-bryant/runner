# 05 run e2e tests Requirements

## Scope

Requirements-only mode: true

Applies to `05_run_e2e_tests.sh`. This is a shared golden orchestration entrypoint owned by the runner engine and exercised end-to-end by the eggnest workspace and consuming repos where its behavioral source/test traceability is enforced. Runner records it here as a self-run inventory entry so the numbered-script coverage and scope-alignment checks remain satisfied without duplicating downstream enforcement.
