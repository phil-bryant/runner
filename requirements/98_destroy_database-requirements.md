# 98 destroy database Requirements

## Scope

Requirements-only mode: true

Applies to `98_destroy_database.sh`. This is a shared golden database entrypoint owned by the runner engine and exercised end-to-end by the consuming repos (teller) where its behavioral source/test traceability is enforced. Runner records it here as a self-run inventory entry so the numbered-script coverage and scope-alignment checks remain satisfied without duplicating downstream enforcement.
