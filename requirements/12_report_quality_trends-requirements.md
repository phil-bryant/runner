# 12 report quality trends Requirements

## Scope

Requirements-only mode: true

Applies to `12_report_quality_trends.sh`. This is a shared golden quality-telemetry entrypoint owned by the runner engine and exercised end-to-end by the consuming repos (teller) where its behavioral source/test traceability is enforced. Runner records it here as a self-run inventory entry so the numbered-script coverage and scope-alignment checks remain satisfied without duplicating downstream enforcement.
