# 10 run classification macos ui Requirements

## Scope

Requirements-only mode: true

Applies to `10_run_classification_macos_ui.sh`. This is a shared golden UI launch entrypoint owned by the runner engine and exercised end-to-end by the consuming repos (classy) where its behavioral source/test traceability is enforced. Runner records it here as a self-run inventory entry so the numbered-script coverage and scope-alignment checks remain satisfied without duplicating downstream enforcement.
