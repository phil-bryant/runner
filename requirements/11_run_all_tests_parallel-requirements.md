# 11 run all tests parallel Requirements

## Scope

Requirements-only mode: true

Applies to `11_run_all_tests_parallel.sh`. This is the shared golden parallel orchestrator owned by the runner engine and exercised end-to-end by the consuming repos (teller/classy/matchy/mailcart) and by runner's own self-run (`run_self_checks.sh`) where its behavioral source/test traceability is enforced. Runner records it here as a self-run inventory entry so the numbered-script coverage and scope-alignment checks remain satisfied without duplicating downstream enforcement.
